{
  "mcpServers": {
    "cortex-memory": {
      "command": "bash",
      "args": [
        "-c",
        "bash \"$1/scripts/ensure-deps.sh\" \"$1\" >/dev/null 2>&1; exec bun \"$1/mcp/server.ts\"",
        "--",
        "${CLAUDE_PLUGIN_ROOT}"
      ]
    }
  }
}
